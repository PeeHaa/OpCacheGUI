<?php $config = new OpCacheGUI\APCu\Configuration(ini_get_all('apcu', false)); ?>

<section id="apcuconfig-tabs" class="tabs">
    <nav id="sub-nav">
        <ul>
            <li class="sna"><a href="#" data-tab="configuration"><?= $this->translator->translate('apcu.config.title'); ?></a></li>
        </ul>
    </nav>
    <section data-content="configuration">
        <table>
            <?php foreach ($config->getIniDirectives() as $key => $directiveItem) { ?>
                <tr>
                    <td><?= $this->translator->translate('config.' . $key); ?></td>
                    <?php if (in_array($key, ['apc.enabled', 'apc.enable_cli', 'apc.coredump_unmap', 'apc.use_request_time', 'apc.rfc1867'])) { ?>
                        <td><span class="<?= $directiveItem ? 'g' : 'r'; ?>s"></span></td>
                    <?php } else { ?>
                        <td><?= htmlspecialchars($directiveItem); ?></td>
                    <?php } ?>
                    <td><p><?= $this->translator->translate('config.' . $key . '.description'); ?></p></td>
                </tr>
            <?php } ?>
        </table>
    </section>
</section>
